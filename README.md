# chrome-extension-with-ts

chrome extension template

## Build

```
$ npm install
$ npm run build
```

## Develop

```
$ npm install
$ npm run dev # watch your /src and build
```

register `/dist` to `chrome://extensions/`
